<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <script>
      window.websocketUrl = "ws://<%= @host %>:<%= @port %>/socket/websocket"
      <% IO.inspect assigns[:img_1] %>
      window.image_1 = <%= if assigns[:img_1] do raw(assigns[:img_1]) else "null" end %>;
      window.image_2 = <%= if assigns[:img_2] do raw(assigns[:img_2]) else "null" end %>;
    </script>

    <title>Hello Resemblixir2!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">

      <div class="jumbotron">
        <h1>Resemblixir</h1>
        <h3>50% less shitty than backstop! (Give or take!)</h3>
      </div>
      <!-- <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p> -->

      <main role="main" id="elm-main">

      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    <script>
      var resemble = require('js/resemble');
      if (window.image_1 && window.image_2) {
        resemble(window.image_1).compareTo(window.image_2).onComplete(function(data) {
          console.log(data);
        })
      }
    </script>
  </body>
</html>
